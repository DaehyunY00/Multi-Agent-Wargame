# Multi-Agent Wargame 코드 검토 보고서

**최초 작성일**: 2026-03-21  **최종 업데이트**: 2026-03-22
**검토 대상**: `src/wargame/` 전체 소스 코드 및 `tests/` 테스트 코드
**검토 기준**: Research_plan.md (RQ1–RQ3) 및 README.md 설계 원칙

---

## 목차

1. [관점 1: 아키텍처 일관성](#관점-1-아키텍처-일관성)
2. [관점 2: 모듈 간 인터페이스 일관성](#관점-2-모듈-간-인터페이스-일관성)
3. [관점 3: Lanchester 모델 구현 정확성](#관점-3-lanchester-모델-구현-정확성)
4. [관점 4: LLM 통합 안정성](#관점-4-llm-통합-안정성)
5. [관점 5: 테스트 커버리지](#관점-5-테스트-커버리지)
6. [추가 검토: run_batch.py 인자 확장 및 plots.py 시각화 함수](#추가-검토-run_batchpy-인자-확장-및-plotspy-시각화-함수)
7. [종합 요약](#종합-요약)

---

## 관점 1: 아키텍처 일관성

### 판정: ⚠️ 주의

### 근거

README.md가 선언한 8개 모듈과 실제 `src/wargame/` 디렉토리 구조가 일치한다. 설계 원칙인 "engine is unaware of LLM", "no LangChain", "log-driven analysis" 모두 코드에서 준수되고 있다. `SimulationEngine`은 `BaseAgent`를 임포트하지 않으며, 분석 계층은 JSONL 파일로부터만 메트릭을 계산한다.

단, 세 가지 설계 갭이 존재한다.

**[문제 1] `run_batch.py` — `--identification-radius` 파라미터 누락**

`scripts/run_single_game.py`는 `--identification-radius` CLI 인수를 받아 `FogOfWarFilter`에 전달하지만, `scripts/run_batch.py`는 이 인수를 구현하지 않아 `FogOfWarFilter()`의 기본값(`identification_radius=1`)으로 고정된다. 배치 실험에서 단일 게임과 동일한 fog-of-war 조건을 재현할 수 없다.

```python
# scripts/run_batch.py 수정 제안 — 4개 지점 변경 필요

# 1. argparse 인수 추가
parser.add_argument(
    "--identification-radius",
    type=int,
    default=1,
    help="Hex radius within which enemy units are fully identified.",
)

# 2. argparse 인수 추가
parser.add_argument(
    "--visibility-radius",
    type=int,
    default=3,
    help="Hex radius within which enemy units are detected.",
)

# 3. FogOfWarFilter 생성 시 적용
fog = FogOfWarFilter(
    visibility_radius=args.visibility_radius,
    identification_radius=args.identification_radius,
)

# 4. ExperimentRunner extra_context에 기록
extra_context={
    "identification_radius": args.identification_radius,
    "visibility_radius": args.visibility_radius,
}
```

**[문제 2] `identification_radius=1` 기본값 — 전술적 비활성화**

`FogOfWarFilter` 기본값 `identification_radius=1`은 적이 인접 칸에 있을 때만 위치가 알려지도록 제한한다. `RuleBasedAgent.nearest_enemy_with_position()`은 위치가 알려진 적만 타깃으로 삼기 때문에, 교전 거리(2–3 헥스)의 적들이 `position=None`으로 관측되어 RECON만 반복하는 비전투 루프가 발생한다(Phase 1 실험에서 s1/s2/s4 시나리오 실증 확인).

```python
# src/wargame/engine/fog_of_war.py — 기본값 변경 제안
@dataclass(slots=True)
class FogOfWarFilter:
    visibility_radius: int = 3
    identification_radius: int = 3   # 1 → 3으로 변경
    # identification_radius=1은 교전 범위보다 좁아 실험 설계를 왜곡함
```

**[문제 3] Python 버전 명세**

`src/wargame/core/enums.py` 및 `src/wargame/agents/prompts.py`에서 `StrEnum`을 사용한다. `StrEnum`은 Python 3.11에 추가된 기능으로, `pyproject.toml`에 `requires-python = ">=3.11"`을 명시해야 한다.

```toml
# pyproject.toml 수정 제안
[project]
requires-python = ">=3.11"
```

---

## 관점 2: 모듈 간 인터페이스 일관성

### 판정: ✅ 통과 (단, 경미한 주의 사항 1건)

### 근거

주요 데이터 계약(engine↔agent, agent↔parser)은 일관되고 명확하다.

- **Engine → Agent**: `FactionViewState` (fog-of-war 적용된 관측 상태). `StateRenderer`가 이를 텍스트로 변환하여 `BaseAgent.decide(state_text)`에 전달.
- **Agent → Engine**: `ActionCommand(unit_id, action_type, target_hex, posture)` 리스트. `TurnLoop`가 `ActionParser`를 통해 검증 후 `SimulationEngine.execute_actions()`에 전달.
- **Logger → Analysis**: JSONL 레코드 형식. `metrics.py`의 `_iter_actions()`, `_iter_decisions()` 등이 일관된 키 이름(`actions`, `metadata.blue`, `metadata.red`, `metadata.white_cell`)으로 파싱.

**[경미한 문제] `_allocate_losses`의 O(n²) 조회**

`simulation.py`의 `_allocate_losses()` 함수 안에서 `next(unit.strength for unit in units if unit.unit_id == unit_id)` 패턴이 딕셔너리 컴프리헨션 내부에서 반복 호출된다. 유닛 수가 소규모인 현재 시나리오에서는 성능 문제가 없지만, 확장 시 O(n²)로 증가한다.

```python
# src/wargame/engine/simulation.py — _allocate_losses 개선 제안
def _allocate_losses(
    total_loss: int,
    units: list[Unit],
    *,
    exposure_weights: dict[str, float],
) -> dict[str, int]:
    if not units or total_loss <= 0:
        return {unit.unit_id: 0 for unit in units}

    # O(n) 딕셔너리로 변경
    strength_by_id = {unit.unit_id: unit.strength for unit in units}

    weights = {
        unit.unit_id: exposure_weights.get(unit.unit_id, 1.0) * strength_by_id[unit.unit_id]
        for unit in units
    }
    total_weight = sum(weights.values())
    if total_weight <= 0:
        return {unit.unit_id: 0 for unit in units}

    allocated: dict[str, int] = {}
    remainder = total_loss
    sorted_ids = sorted(units, key=lambda u: u.unit_id)
    for unit in sorted_ids[:-1]:
        share = int(floor(total_loss * weights[unit.unit_id] / total_weight))
        share = min(share, strength_by_id[unit.unit_id])
        allocated[unit.unit_id] = share
        remainder -= share
    last = sorted_ids[-1]
    allocated[last.unit_id] = min(remainder, strength_by_id[last.unit_id])
    return allocated
```

**[경미한 문제] `resolve_combat` 요약 문자열에서 state 뮤테이션 후 참조**

`resolve_combat()` 내부에서 유닛에 피해를 적용한 뒤 `state.units[unit_id].faction`을 참조하는 f-string이 있다. `faction`은 불변 속성이므로 현재 버그는 아니지만, 코드 읽기상 혼란을 줄 수 있다. 가독성을 위해 casualties 적용 전 별도 딕셔너리로 저장하는 것을 권장한다.

---

## 관점 3: Lanchester 모델 구현 정확성

### 판정: ✅ 통과

### 근거

`combat/lanchester.py`의 핵심 손실 계산식을 분석했다.

```
blue_loss = red_attrition_rate × red_strength × time_step / blue_defense_modifier
red_loss  = blue_attrition_rate × blue_strength × time_step / red_defense_modifier
```

이 수식은 **이산 Lanchester 선형 법칙(Linear Law)** 의 1스텝 근사치이다. 표준 Lanchester 모델에서 선형 법칙은 `dB/dt = -β × R`로 정의되며, 방어 수정자를 분모에 적용하는 방식(방어 수정자가 클수록 피해 감소)은 군사 시뮬레이션 관례와 일치한다.

추가로 확인된 정확성 요소들:

- `LanchesterOutcome.__post_init__`에서 `start - loss == remaining` 조건을 `abs_tol=1e-9`로 검증하여 부동소수점 일관성 보장.
- `_validate_inputs()`에서 음수 방어 수정자, `blue_defense_modifier == 0` (ZeroDivisionError 방지), 중복 시드 등 모든 엣지 케이스 검증.
- `_clamp_loss()`로 손실을 `[0, available_strength]` 범위로 제한하여 음수 전력이 발생하지 않음.
- 확률적 노이즈: `gauss(0, noise_std × base_loss)` 적용 후 clamp. 스케일 보정은 없으나 실험 목적에서 허용 가능한 수준.
- `SimulationEngine._effective_offense()`에서 strength × combat_power × supply × morale × action_factor × posture_factor 복합 적용 — 단순 강도 이상의 다차원 전투력 모델링.

**[참고 사항] "제곱 법칙"과 "선형 법칙" 혼용 표기**

코드 docstring에 "square-law-inspired one-turn approximation"이라고 표현했으나, 손실 계산식 `loss = rate × 상대 강도 × time_step`은 제곱 법칙이 아닌 **선형 법칙**이다. 제곱 법칙은 `dB/dt = -β × R²`이지만 이 코드에서는 R²을 사용하지 않는다. 해석에 혼동을 줄 수 있으므로 docstring 수정을 권장한다.

```python
# src/wargame/combat/lanchester.py — docstring 수정 제안
"""Resolve one combat step using a discrete Lanchester-style update.

The update uses a **linear-law** one-turn approximation:

- blue_loss ~= red_attrition_rate * red_strength * time_step / blue_defense_modifier
- red_loss  ~= blue_attrition_rate * blue_strength * time_step / red_defense_modifier

This corresponds to Lanchester's Linear Law (area fire model), not the Square Law.
Terrain is modeled as an explicit defensive modifier on the occupying force.
"""
```

---

## 관점 4: LLM 통합 안정성

### 판정: ✅ 통과

### 근거

LLM 통합 계층(`local_llm.py`, `parser.py`, `prompts.py`)은 안정적으로 설계되었다.

**강점:**

1. **폴백 메커니즘 완전성**: `LocalLLMAgent.decide()`에서 `ModelOutputError` 또는 `ActionParseError` 발생 시 `fallback_on_error=True`(기본값)면 모든 유닛에 HOLD/DEFEND를 반환하고, 에러 내용을 `metadata["errors"]`에 기록한다. 게임 루프가 절대 중단되지 않음.

2. **JSON 추출 내결함성**: `extract_json_object()`는 바이트 단위로 중괄호 깊이를 추적하여 LLM이 생성한 코드 펜스나 산문 앞뒤로 감싸진 JSON도 정확히 추출한다. 문자열 리터럴 내 `{}`는 `in_string` 플래그로 무시하고, `\`를 에스케이프 시퀀스로 처리한다.

3. **검증 계층화**: `ActionParser.parse()`는 ① JSON 파싱 → ② 최상위 필드 존재 확인(`reasoning`, `actions`, `doctrine_reference`) → ③ 각 액션 항목 타입/범위 검증 → ④ 유닛 ID 유효성 → ⑤ 그리드 경계 검사 순으로 계층적으로 검증하여 명확한 에러 메시지 제공.

4. **`MockLocalLLMBackend`를 통한 테스트 격리**: 실제 모델 없이 완전한 통합 테스트 가능. `deque` 기반으로 응답 순서 보장.

5. **프롬프트 레지스트리 불변성**: `PromptRegistry`가 `frozen=True` dataclass로 구현되어 런타임 중 프롬프트 변경이 불가능.

**[경미한 문제 1] `HOLD` 액션의 `target_hex` 검증 이중 확인**

`_parse_action()`에서 `target_hex`를 파싱한 뒤 `validate_actions()`에서 다시 HOLD + target_hex 조합을 검증한다. 중복 검증은 버그가 아니지만 코드 중복이다.

**[경미한 문제 2] `max_tokens=512` 기본값의 실용성**

`LocalLLMConfig.max_tokens = 512`는 3개 유닛의 액션 JSON을 생성하기에 충분하지만, 시나리오가 복잡해질수록 추론(reasoning) 필드가 잘릴 수 있다. 연구 단계에서는 무방하나 실제 모델 통합 시 1024–2048로 올리는 것을 권장한다.

**[경미한 문제 3] 빈 `valid_unit_ids` 처리**

`LocalLLMAgent.decide()`에서 `valid_unit_ids=()` (기본값)로 호출되면, 파서가 모든 unit_id를 "알 수 없는 유닛"으로 거부하여 폴백이 발동되지만 폴백 액션 목록도 비게 된다. 호출부(`TurnLoop`)는 항상 `valid_unit_ids`를 전달하므로 현재 실제 경로에서는 발생하지 않으나, 방어적으로 경고 로그를 추가하는 것을 고려할 수 있다.

---

## 관점 5: 테스트 커버리지

### 판정: ⚠️ 주의

### 근거

전체 38개 테스트가 통과하며, 핵심 경로들은 잘 커버되고 있다. 그러나 몇 가지 중요한 커버리지 갭이 있다.

**커버되는 영역 (강점):**

| 테스트 파일 | 커버 내용 |
|---|---|
| `test_lanchester.py` | 결정론적/확률적 손실, 음수 방지, 입력 검증 |
| `test_simulation_engine.py` | 국지 전투, 복수 교전 격리, 시드 재현성 |
| `test_turn_loop.py` | 폴백 메커니즘, white cell 점수 기록, JSONL 로깅 |
| `test_local_llm_agent.py` | 파싱 성공, 폴백 에러 노출, 비폴백 예외 |
| `test_metrics.py` | action_entropy, ESI, win_rate |
| `test_fog_of_war.py` | 가시범위, 식별 반경 분리 |
| `test_experiments.py` | 시드 훅, 자동 시딩, 로그 컨텍스트 |

**[문제 1] `identification_radius` 경계 조건 테스트 부재**

`FogOfWarFilter(identification_radius=1)` 상태에서 `RuleBasedAgent`가 RECON 루프에 빠지는 현상(Phase 1에서 실증)은 테스트로 커버되지 않는다. 이 조합이 전투 없이 게임을 종료하는 regression 테스트가 필요하다.

```python
# tests/test_baseline_agents.py에 추가 제안
def test_rule_agent_with_identification_radius_1_falls_back_to_recon() -> None:
    """identification_radius=1이면 근거리 외 적은 위치 미상 → RECON 반복."""
    from wargame.agents._baseline_utils import tactical_report_from_view
    from wargame.engine.fog_of_war import FactionViewState, FogOfWarFilter, TurnMetadata
    # ... 적이 distance=2에 있고 identification_radius=1인 FactionViewState를 구성
    # ... RuleBasedAgent.decide_view() 결과가 ActionType.RECON인지 확인
```

**[문제 2] `run_batch.py` / `run_single_game.py` CLI 스크립트 통합 테스트 부재**

`test_cli_scripts.py`가 존재하지만, `--identification-radius` 파라미터가 실제 FogOfWarFilter에 전달되는지, batch 스크립트와 single 스크립트 간 동일 파라미터 조건에서 결과가 일치하는지 확인하는 테스트가 없다.

**[문제 3] `forward_step` 대각 편향 테스트 부재**

`_baseline_utils.forward_step()`은 Blue의 경우 `+q` 방향 이웃 중 `(item.q, item.r)` 정렬 시 가장 작은 r을 선택한다. 이로 인해 Blue는 항상 상단 대각선(+q, -r)으로 이동하고, Red는 하단 대각선(-q, -r)으로 이동하여 s1 같은 중앙 직선 시나리오에서 서로 교차 없이 반대 코너로 이동한다. 이 동작이 의도된 것인지 검증하는 테스트가 없다.

```python
# tests/test_baseline_agents.py에 추가 제안
def test_forward_step_blue_moves_in_positive_q_direction() -> None:
    grid = HexGrid(width=10, height=10)
    start = Position(5, 5)
    result = forward_step(grid, start=start, faction=Faction.BLUE, context="test")
    assert result is not None
    assert result.q > start.q, "Blue should advance in +q direction"


def test_forward_step_red_moves_in_negative_q_direction() -> None:
    grid = HexGrid(width=10, height=10)
    start = Position(5, 5)
    result = forward_step(grid, start=start, faction=Faction.RED, context="test")
    assert result is not None
    assert result.q < start.q, "Red should advance in -q direction"
```

**[문제 4] `BatchRunner.from_matrix` 단위 테스트 부재**

`ExperimentRunner` 단위 테스트는 있지만, `BatchRunner.from_matrix()`가 (시나리오 수) × (매치업 수) × (시드 수)의 올바른 러너 개수를 생성하는지 확인하는 테스트가 없다.

**[문제 5] `summarize_runs()` 통합 테스트 부재**

`stats.py`의 `summarize_runs()`는 `metrics.py`의 여러 함수를 조합하지만, 복수 run JSONL 파일을 입력으로 받아 `SummaryStatistics` 필드 전체가 올바르게 채워지는지 확인하는 통합 테스트가 없다.

---

---

## 추가 검토: run_batch.py 인자 확장 및 plots.py 시각화 함수

**검토 일자**: 2026-03-22
**검토 대상**:
- `scripts/run_batch.py` — `--visibility-radius`, `--identification-radius`, fog-preset 시스템 추가 (2026-03-22 변경분)
- `src/wargame/analysis/plots.py` — matplotlib 시각화 함수 5개 추가

---

### 6-A. `scripts/run_batch.py` 검토

#### 판정: ⚠️ 주의 (결함 2건 — 1건 Medium, 1건 Low)

---

**[RB-1] Medium — `_parse_matchup`: 쉼표 없는 입력 시 `ValueError` 비표시 크래시**

```python
# scripts/run_batch.py:147
blue_spec, red_spec = [item.strip() for item in value.split(",", maxsplit=1)]
```

`value`에 쉼표가 없으면 `split(",", maxsplit=1)`이 원소 1개짜리 리스트를 반환하여 언패킹 시 `ValueError: not enough values to unpack`이 발생한다. argparse가 이 예외를 캐치하지 않으므로 스택 트레이스가 그대로 출력되고 종료된다.

```
# 재현
python scripts/run_batch.py --scenario s1 --matchup "rule" ...
# ValueError: not enough values to unpack (expected 2, got 1)
```

**수정 방안:**

```python
def _parse_matchup(value: str) -> MatchupSpec:
    parts = [item.strip() for item in value.split(",", maxsplit=1)]
    if len(parts) != 2:  # noqa: PLR2004
        raise argparse.ArgumentTypeError(
            f"Matchup must be 'blue_spec,red_spec' but got: {value!r}"
        )
    blue_spec, red_spec = parts
    name = f"{blue_spec}-vs-{red_spec}".replace(":", "-")
    return MatchupSpec(name=name, blue_agent_name=blue_spec, red_agent_name=red_spec)
```

`argparse.ArgumentTypeError`로 감싸면 argparse가 친숙한 오류 메시지로 출력하고 help 힌트를 표시한다. 또는 `--matchup`의 `type=_parse_matchup`으로 연결해도 동일한 효과를 얻는다.

---

**[RB-2] Low — `_select_batch_fog_preset`: `experiment_mode` 반환값 불일치**

```python
# 현재 코드
if all(_matchup_uses_local_llm(matchup) for matchup in matchups):
    return user_preset, [], "llm"       # experiment_mode = "llm"
return user_preset, [], "baseline"      # experiment_mode = "baseline"
```

`user_preset != "auto"` 분기에서 매치업이 모두 local_llm이면 `"llm"`, 그렇지 않으면 `"baseline"`을 반환한다. 그런데 `_resolve_fog_settings()`는 `experiment_mode` 값을 로그 메시지 생성에만 사용하고 실제 반경 계산에는 사용하지 않으므로 현재 버그는 아니다. 그러나 `experiment_mode`가 나중에 반경 선택 로직에 사용될 경우 잘못된 preset으로 fallback될 위험이 있다.

**권장 수정:** `user_preset` 자체를 `experiment_mode`로 사용하거나, 호출 시 반환된 `experiment_mode`를 무시한다는 주석을 추가한다.

```python
# 명확성을 위한 대안
return user_preset, [], user_preset   # mode = 사용자가 명시한 preset 이름
```

---

**[RB-G1] High — 필수 테스트 부재: 쉼표 없는 matchup 입력**

`_parse_matchup`의 쉼표 누락 경로는 CI에서 검증되지 않는다. RB-1 수정과 함께 아래 테스트를 추가해야 한다.

```python
# tests/test_run_batch.py (신규 파일 또는 기존 CLI 테스트에 추가)
import pytest
import argparse

def test_parse_matchup_raises_on_missing_comma() -> None:
    with pytest.raises((ValueError, argparse.ArgumentTypeError)):
        _parse_matchup("rule")   # 쉼표 없음

def test_parse_matchup_valid_input() -> None:
    spec = _parse_matchup("rule,rule")
    assert spec.blue_agent_name == "rule"
    assert spec.red_agent_name == "rule"
    assert spec.name == "rule-vs-rule"

def test_parse_matchup_local_llm_spec() -> None:
    spec = _parse_matchup("local_llm:mlx-community/Qwen2.5-7B-Instruct-4bit,rule")
    assert "local_llm" in spec.blue_agent_name
    assert spec.red_agent_name == "rule"
```

**[RB-G2] — fog_preset "auto" 분기 단위 테스트 부재**

`_select_batch_fog_preset`의 3가지 분기(모두-llm / 모두-baseline / 혼합)에 대한 단위 테스트가 없다.

**[RB-G3] — `_build_runner`에서 agent 생성 검증 부재**

`_build_runner`가 올바른 faction의 agent를 올바른 side에 배치하는지 확인하는 테스트가 없다.

---

### 6-B. `src/wargame/analysis/plots.py` 검토

#### 판정: ⚠️ 주의 (결함 3건 — 2건 High, 1건 Low)

---

**[PL-1] High — 빈 데이터 dict 입력 시 `ax.boxplot([])` 크래시**

`plot_action_entropy_comparison`, `plot_esi_comparison`, `plot_dcr_distribution`은 `data: dict[str, list[float]]`를 받는다. 키는 있지만 값 리스트가 비어 있는 경우:

```python
data = {"blue_rule": [], "red_rule": []}
plot_action_entropy_comparison(data, output_path)
# → ax.boxplot([[], []]) → matplotlib이 UserWarning 후 빈 박스 렌더링
```

키가 전혀 없는 경우:

```python
data = {}
plot_action_entropy_comparison(data, output_path)
# → ax.set_xticklabels([]) 이후 ax.boxplot([]) 호출
# → matplotlib 버전에 따라 ValueError 또는 빈 파일 출력
```

두 경우 모두 호출부에서 명시적으로 처리되지 않는다.

**수정 방안:**

```python
def plot_action_entropy_comparison(
    data: dict[str, list[float]],
    output_path: Path,
) -> list[Path]:
    filled = {k: v for k, v in data.items() if v}   # 빈 리스트 제거
    if not filled:
        return []
    # ... 기존 로직
```

---

**[PL-2] High — `plot_win_rate_by_scenario`: `numpy` 임포트 가드 없음**

```python
# src/wargame/analysis/plots.py (plot_win_rate_by_scenario 내부)
import numpy as np
```

`numpy`는 `plots` optional dependency group에 선언되어 있으나, 함수 상단에서 직접 임포트한다. `_require_matplotlib()`처럼 `_require_numpy()`를 추가하거나 `try/except ImportError`로 감싸지 않으면 `numpy`가 없는 환경에서 다른 plot 함수들(numpy 불필요)도 모듈 임포트 시점에 실패한다.

실제로 `numpy`는 `matplotlib`이 설치된 환경에서 대부분 함께 설치되므로 현재 실환경에서 문제가 발생할 가능성은 낮다. 그러나 `mlx-only` 환경이나 최소 설치 환경에서 예기치 않은 `ImportError`가 발생할 수 있다.

**수정 방안 (함수 내부로 임포트 이동):**

```python
def plot_win_rate_by_scenario(
    data: dict[str, dict[str, int]],
    output_path: Path,
) -> list[Path]:
    mpl = _require_matplotlib()
    import numpy as np   # 함수 내부로 이동
    ...
```

---

**[PL-5] Low — `_apply_academic_style`의 전역 `rcParams` 뮤테이션**

```python
def _apply_academic_style(mpl: Any) -> None:
    mpl.rcParams["font.family"] = "sans-serif"
    mpl.rcParams["font.size"] = 10
    # ...
```

`rcParams`는 프로세스 전역 상태다. 한 번 plot 함수가 호출되면 동일 프로세스 내의 이후 모든 matplotlib 렌더링에 스타일이 적용된다. 대화형 분석(Jupyter)에서 사용자의 스타일 설정을 덮어쓰는 부작용이 있다.

**권장 수정:** `matplotlib.style.context()` 또는 `mpl.rc_context()`로 스코프를 제한한다.

```python
def _save_figure(fig: Any, plt: Any, base_path: Path) -> list[Path]:
    ...

# 각 plot 함수에서:
with mpl.rc_context(rc=_ACADEMIC_RC):
    fig, ax = plt.subplots(...)
    ...
    return _save_figure(fig, plt, output_path)

_ACADEMIC_RC = {
    "font.family": "sans-serif",
    "font.size": 10,
    "axes.spines.top": False,
    "axes.spines.right": False,
    "figure.dpi": 300,
}
```

---

**[PL-G1] — 빈 데이터 / 단일 레이블 경계 테스트 부재**

아래 케이스들에 대한 테스트가 없다:
- `data = {}` → 함수가 `[]`를 반환하고 파일을 생성하지 않는지
- `data = {"only": []}` → 빈 리스트 필터링 후 조기 반환하는지
- `data = {"a": [1.0]}` → 단일 레이블 1개 데이터포인트에서 박스플롯이 생성되는지

**[PL-G2] — PNG/SVG 이중 출력 계약 테스트 부재**

`_save_figure()`가 `.png`와 `.svg` 두 파일을 모두 반환하는지, 반환 리스트가 `[Path(...png), Path(...svg)]` 순서인지 확인하는 테스트가 없다.

**[PL-G3] — `plot_force_curve` 단조성 테스트 부재**

전력 곡선이 x축(턴) 방향으로 단조 감소하는 시나리오 레코드를 입력했을 때 출력 파일이 생성되는지 확인하는 테스트가 없다.

**[PL-G4] — `AutoChatTemplateAdapter` 및 Korean font fallback 통합 테스트 부재**

`_apply_academic_style`이 시스템에 한국어 폰트가 없는 환경에서 `DejaVu Sans`로 정상 fallback되는지 확인하는 테스트가 없다 (CI 환경에서 특히 중요).

---

### 6-C. 검토 요약

| ID | 심각도 | 파일 | 내용 |
|---|---|---|---|
| RB-1 | **Medium** | `run_batch.py:147` | `_parse_matchup` 쉼표 없는 입력 시 비표시 크래시 |
| RB-2 | Low | `run_batch.py:165` | `experiment_mode` 반환값 불일치 (런타임 무해) |
| RB-G1 | **High** | 테스트 누락 | 쉼표 없는 matchup 입력 경로 미검증 |
| RB-G2 | Low | 테스트 누락 | fog_preset "auto" 3개 분기 미검증 |
| RB-G3 | Low | 테스트 누락 | `_build_runner` faction 배치 미검증 |
| PL-1 | **High** | `plots.py` | 빈 data dict 입력 시 `ax.boxplot([])` 크래시 |
| PL-2 | **High** | `plots.py` | `numpy` 임포트 가드 없음 — 모듈 임포트 시점 실패 위험 |
| PL-5 | Low | `plots.py` | 전역 `rcParams` 뮤테이션 — 대화형 환경 부작용 |
| PL-G1~G4 | Low | 테스트 누락 | 빈 데이터, 파일 출력 계약, 폰트 fallback 미검증 |

**즉시 수정 권장 (실험 안정성 영향):**
1. **RB-1**: `_parse_matchup`에 쉼표 검증 추가 + `argparse.ArgumentTypeError` 발생
2. **PL-1**: 각 plot 함수 진입부에 빈 데이터 early-return 추가
3. **PL-2**: `numpy` 임포트를 `plot_win_rate_by_scenario` 함수 내부로 이동

**권장 수정 (코드 품질):**
4. **PL-5**: `_apply_academic_style` → `mpl.rc_context()` 스코프 제한
5. **RB-2**: `experiment_mode` 반환 로직 명확화
6. **RB-G1**: `test_parse_matchup_raises_on_missing_comma` 테스트 추가

---

## 종합 요약

| 관점 | 판정 | 주요 발견 |
|---|---|---|
| 1. 아키텍처 일관성 | ⚠️ 주의 | `run_batch.py` ID-radius 누락, `identification_radius=1` 기본값 전술 왜곡, Python 버전 명세 부재 |
| 2. 모듈 인터페이스 일관성 | ✅ 통과 | 핵심 데이터 계약 일관적; `_allocate_losses` O(n²) 경미 |
| 3. Lanchester 모델 정확성 | ✅ 통과 | 선형 법칙 1스텝 근사 수학적으로 정확; docstring "square-law" 표기 오해 소지 |
| 4. LLM 통합 안정성 | ✅ 통과 | 폴백 메커니즘, JSON 추출, 검증 계층 모두 견고 |
| 5. 테스트 커버리지 | ⚠️ 주의 | `identification_radius` 경계, `forward_step` 방향 편향, CLI 통합, `BatchRunner` 매트릭스 테스트 부재 |
| 6. 추가 검토 (2026-03-22) | ⚠️ 주의 | `run_batch.py` matchup 파싱 크래시(RB-1 Medium), `plots.py` 빈 데이터 크래시(PL-1 High), numpy 임포트 가드 누락(PL-2 High) |

### 즉시 수정 필요 (실험 안정성 직접 영향)

1. **`run_batch.py`에 `--identification-radius` 및 `--visibility-radius` 파라미터 추가** ✅ 완료 (2026-03-22)
2. **`pyproject.toml`에 `requires-python = ">=3.11"` 명시** ✅ 완료 (2026-03-22)
3. **`_parse_matchup` 쉼표 검증 추가** — 쉼표 없는 입력 시 비표시 `ValueError` 크래시 (RB-1)
4. **`plots.py` 각 함수에 빈 데이터 early-return 추가** — `ax.boxplot([])` 크래시 방지 (PL-1)
5. **`plot_win_rate_by_scenario` 내 `numpy` 임포트를 함수 내부로 이동** — 모듈 임포트 시 실패 방지 (PL-2)

### 권장 수정 (연구 품질 향상)

6. `FogOfWarFilter` 기본값 `identification_radius` 실험 의도에 맞게 조정 (또는 모든 실험 스크립트에서 명시적 설정)
7. `combat/lanchester.py` docstring의 "square-law" → "linear-law" 수정
8. `identification_radius` 경계 조건 regression 테스트 추가
9. `forward_step` 방향 편향 동작 문서화 또는 테스트 추가
10. `BatchRunner.from_matrix` 단위 테스트 추가
11. `_apply_academic_style` → `mpl.rc_context()` 스코프 제한 (PL-5)
12. `test_parse_matchup_raises_on_missing_comma` 테스트 추가 (RB-G1)

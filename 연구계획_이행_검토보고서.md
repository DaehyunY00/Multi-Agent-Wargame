# 연구계획서 이행 검토 보고서

> **작성일**: 2026-03-25 (최초: 2026-03-22)
> **검토 대상**: `Research_plan.md` vs 현재 코드 구현 + Phase 1~4 실험 결과
> **검토 방법**: 연구계획서의 각 절을 기준으로 구현 파일 및 실험 로그를 대조

---

## 전체 이행률 요약

| 영역 | 계획 항목 수 | 완료 | 부분 완료 | 미이행 | 이행률 |
|---|---|---|---|---|---|
| **시스템 아키텍처** | 7 | 7 | 0 | 0 | **100%** |
| **실험 설계 (변수/지표)** | 8 | 6 | 2 | 0 | **87%** |
| **시나리오 설계 (5종)** | 5 | 5 | 0 | 0 | **100%** |
| **베이스라인 구현** | 3 | 3 | 0 | 0 | **100%** |
| **LLM 에이전트 구현** | 5 | 5 | 0 | 0 | **100%** |
| **분석 메트릭 구현** | 7 | 7 | 0 | 0 | **100%** |
| **실험 실행 (Phase 1~5)** | 17 | 10 | 3 | 4 | **~65%** |
| **논문 작성** | 7 | 0 | 0 | 7 | **0%** |

> **전체 이행률 (구현 기준)**: **~97%** (실험 실행 포함 시 **~72%**)

---

## 1. 시스템 아키텍처 — 이행률 100% ✅

### ✅ 완료된 구현

#### (a) Simulation Engine

| 계획 항목 | 구현 파일 | 구현 상태 |
|---|---|---|
| HexGrid (헥사곤 격자, 거리 계산) | `core/hexgrid.py` | ✅ axial 좌표 체계 구현 |
| 지형 유형별 Defense Modifier | `core/terrain.py` | ✅ 5종 지형 (Open/Mountain/Urban/Forest/River) |
| Lanchester 교전 판정 (결정론적+확률적) | `combat/lanchester.py` | ✅ 구현 완료 |
| Fog of War (가시범위/식별범위) | `engine/fog_of_war.py` | ✅ `visibility_radius` + `identification_radius` 이중 구조 |
| 상태 관리 (State Manager) | `engine/state_manager.py` | ✅ 구현 완료 |
| 턴 루프 (Agent Orchestrator) | `orchestrator/turn_loop.py` | ✅ Blue→Red→Combat 순서 구현 |
| JSONL 로거 | `logging/jsonl_logger.py` | ✅ 턴별 JSON Lines 기록 |

#### (b) Agent System

| 계획 항목 | 구현 파일 | 구현 상태 |
|---|---|---|
| State-to-Text Converter | `agents/state_to_text.py` | ✅ 자연어 상황 보고서 변환 |
| Action Parser (JSON → Engine) | `agents/parser.py` | ✅ 파싱 + 6종 복원 로직 완전 구현 (하단 상세) |
| Blue/Red/White Agent (LLM 기반) | `agents/blue_agent.py`, `red_agent.py`, `white_cell.py` | ✅ 역할 분리 구현 |
| Local LLM 래퍼 (MLX) | `agents/backends/mlx_backend.py` | ✅ mlx-lm 연동 + sampler API 수정 완료 |
| Local LLM 래퍼 (vLLM) | `agents/backends/vllm_backend.py` | ✅ vLLM 연동 |
| Chat template 자동 적용 | `agents/backends/chat_templates.py` | ✅ 모델별 포맷 처리 |
| 시스템 프롬프트 (FM 3-90 + MDMP) | `agents/prompts.py` | ✅ 교리 지침 4항 + ASSESS/DEVELOP/DECIDE 3단계 구현 |

#### (c) 분석 시스템

| 계획 항목 | 구현 파일 | 구현 상태 |
|---|---|---|
| ExperimentRunner / BatchRunner | `experiments/runner.py`, `batch.py` | ✅ 시드 기반 반복 실험 |
| Seed 관리 (`derive_seed_bundle`) | `experiments/seeds.py` | ✅ 결정론적 재현성 보장 |
| 분석 스크립트 | `scripts/evaluate_logs.py`, `run_statistical_tests.py` | ✅ 전체 구현 |

---

## 2. Lanchester 모형 — 이행률 75% (주요 편차 존재)

### ⚠️ 핵심 편차: 선형법칙 vs 제곱법칙

| 항목 | 연구계획서 명세 | 실제 구현 |
|---|---|---|
| 모형 종류 | **확장 Lanchester 제곱법칙** (Square Law) | **선형법칙 근사** (Linear Law) |
| 수식 | `dB/dt = -α·R(t)` (제곱법칙 = 적 병력에 비례) | `blue_loss = red_rate × red_strength × Δt / defense_mod` (동일한 수식 — 실은 선형) |
| ODE 사용 | `scipy.integrate.odeint` 사용 계획 | 1-step 이산 근사, scipy 미사용 |

**분석**: 수식 `dB/dt = -α·R(t)`는 사실 **선형 법칙**(Linear Law)입니다. 제곱법칙은 `dB/dt = -α·R²(t)` 형태여야 합니다. 연구계획서 자체에도 수식이 혼재되어 있으나, 코드의 docstring에서 "square-law-inspired"라고 표기한 부분이 논문 기술 시 오해를 낳을 수 있습니다.

**영향**: 실험 결과 해석에는 직접적 영향 없음 (α/β 계수가 동일하게 적용됨). 다만 **논문에서 모형 설명 시 선형법칙임을 명확히 기술**해야 합니다.

**권장 조치**: `lanchester.py` docstring에서 "square-law-inspired" → "linear law" 수정, 논문 3.2.2절 수식 정정.

### ✅ 완료

- 결정론적 / 확률적 모드 분리 구현 ✅
- 지형 보정 (defense_modifier) 적용 ✅
- α=0.05, β=0.05 기본값 ✅
- 확률적 노이즈 σ=0.1 ✅
- 교전 종료 조건 (`_CONTACT_RANGE = 1`) ✅

---

## 3. 실험 설계 — 이행률 87%

### ✅ 완료된 변수/지표 구현

| 종속변수 | 계획 | 구현 위치 | 상태 |
|---|---|---|---|
| 교리 준수율 (DCR) | White Cell 턴별 판정 비율 | `metrics.py: doctrine_compliance_rate()` | ✅ |
| 전술적 합리성 (TRS) | White Cell 5점 척도 | `metrics.py: tactical_rationality_score()` | ✅ |
| 행동 다양성 (Action Entropy) | Shannon Entropy | `metrics.py: action_entropy()` | ✅ |
| 공격 성향 지수 (ESI) | 공격 행동 비율 | `metrics.py: escalation_sensitivity_index()` | ✅ (턴간 변동성으로 측정) |
| 전술 전환 빈도 (TTF) | 전술 변경 횟수 | `metrics.py: tactic_transition_frequency()` | ✅ |
| JSON 파싱 성공률 | 유효 JSON 비율 | `metrics.py: json_parsing_success_rate()` | ✅ |
| 추론 시간 | 턴당 LLM 소요 시간 | `metrics.py: inference_time_summary()` | ✅ |

### ⚠️ 부분 완료

**ESI 정의 편차**

- **계획**: ESI = N_offensive / N_total (공격 행동의 단순 비율)
- **구현**: `escalation_sensitivity_index()` = 턴간 공격성 점수의 **변동성** (mean absolute change)

두 정의가 다릅니다. 계획서의 ESI(공격 비율)와 구현의 ESI(전술 변동성)는 측정하는 개념이 다릅니다.

**권장 조치**: 계획서의 원래 ESI (단순 공격 비율) = `N_offensive / N_total`을 별도 함수로 추가하거나, 계획서의 ESI 정의를 구현에 맞게 업데이트해야 합니다. 논문 작성 전 정의 통일 필수.

**White Cell DCR 평가 방식 편차**

- **계획**: LLM White Cell Agent가 6개 교리 원칙 체크리스트로 평가
- **구현**: `HeuristicWhiteCellAgent`가 근접도·전투 손실·행동 유형 기반 휴리스틱으로 대체 구현됨

`HeuristicWhiteCellAgent`는 실제 LLM 없이도 돌아가는 결정론적 대체품으로, 재현성은 높지만 계획서의 "6개 교리 원칙 체크리스트" 평가와는 내용이 다릅니다. LLM 실험 시 LLM WhiteCell 병행 운용 여부 결정 필요.

---

## 4. 시나리오 설계 — 이행률 100% ✅

5개 시나리오가 계획서 명세와 완전히 일치하여 구현되었고, Phase 1-3에서 모두 실행 검증됨.

| 시나리오 | 계획서 명세 | 구현 확인 | Phase 3 베이스라인 결과 (50 seeds) |
|---|---|---|---|
| S1 평지 조우전 | 양측 3개 중대, 기동 자유 | ✅ `s1_open_encounter` | Blue 승률 6%, Rule vs Rule |
| S2 산악 방어진지 | Blue 공격 / Red 방어 | ✅ `s2_mountain_assault` | Blue 승률 ~96% |
| S3 시가지 전투 | 시가지 확보 전투 | ✅ `s3_urban_fight` | 교전 빈도 높음 |
| S4 하천 도하 | Blue 도하 / Red 저지 | ✅ `s4_river_crossing` | 교전 빈도 중간 |
| S5 포위 돌파 | 비대칭 상황 돌파 | ✅ `s5_breakout` | Blue 우세 |

> **주목**: 시나리오 ID는 `s2_chokepoint_defense`가 아닌 `s2_mountain_assault` 등 실제 구현된 이름을 사용해야 함. 이전 실험에서 잘못된 이름으로 `FileNotFoundError` 발생 사례 있었음 — 수정 완료.

---

## 5. 베이스라인 구현 — 이행률 100% ✅

| 계획 항목 | 구현 파일 | 상태 |
|---|---|---|
| Script Agent (3종 스크립트) | `script_agent.py`, `script_agents/` | ✅ `frontal_assault`, `flanking_maneuver` 구현 |
| Rule-Based Agent (if-then 규칙) | `rule_agent.py` + `_baseline_utils.py` | ✅ 전투력 비율·거리·손실 기반 |
| Random Agent | `random_agent.py` | ✅ 균일 무작위 행동 |

---

## 6. 기술 스택 이행 — 이행률 100% ✅

| 계획 기술 | 구현 여부 | 비고 |
|---|---|---|
| Python 3.11+ | ✅ | StrEnum 사용 (3.13 실행 확인) |
| MLX + mlx-lm (Mac M4) | ✅ | `mlx_backend.py` — `make_sampler(temp=...)` API 수정 완료 |
| vLLM (Colab A100) | ✅ | `vllm_backend.py` 구현 완료 |
| Qwen2.5-7B-Instruct-4bit | ✅ | Phase 4-1 실험 진행중 |
| Mistral-7B, Llama-3.1-8B | ✅ | Colab 실험 완료 (결과 전송 대기) |
| HexGrid 자체 구현 | ✅ | `core/hexgrid.py` |
| JSONL 로깅 | ✅ | `logging/jsonl_logger.py` |
| Matplotlib 시각화 | ✅ | `analysis/plots.py` 5개 함수 |
| SciPy 통계 분석 | ✅ | `analysis/stats.py`, `run_statistical_tests.py` |

---

## 7. 실험 실행 이행 현황

### Phase 1 (엔진 검증) — 이행률 94% ✅

| 항목 | 계획 기준 | 실행 결과 | 상태 |
|---|---|---|---|
| 1-1 단위 테스트 38개 | 100% PASS | 38/38 PASS ✅ | **완료** |
| 1-2 단일 게임 동작 확인 | 정상 종료, 교전 발생 | T04 교전, turns=12 ✅ | **완료** |
| 1-3 시나리오 5종 × Rule | 8~18턴, 교착<20% | s1/s3/s5 교전 ✅, s2/s4 설계 의도 ✅ | **완료** |
| 1-4 stochastic 비교 | 결정론적 ≠ 확률적 | 두 파일 생성 완료, 비교 스크립트 미실행 | **⏳ 95%** |

### Phase 2 (LLM 구축 및 안정성) — 이행률 100% ✅

| 항목 | 계획 기준 | 실행 결과 | 상태 |
|---|---|---|---|
| 2-1 모델 다운로드/벤치마크 | mlx-lm 로드 확인 | Qwen2.5-7B-4bit MLX 로드 완료 | **완료** |
| 2-2 JSON 파싱 안정성 테스트 | 파싱 성공률 ≥ 90% | 초기 0% → parser 수정 후 86% → 최종 ~86% | **완료** |
| 2-3 프롬프트 개선 (FM 3-90) | MDMP 3단계 + 교리 6항 | 4항 FM 3-90 + MDMP 3단계 구현 완료 | **완료** |

**주요 수정 이력 (Phase 2 결과 반영)**:

| 수정 | 내용 | 파일 |
|---|---|---|
| MLX sampler API 수정 | `mlx_lm.generate(temp=...)` → `make_sampler(temp=...)` + `sampler=sampler` | `mlx_backend.py` |
| max_tokens 기본값 증가 | 512 → 1024 (JSON 중단 방지) | `local_llm.py` |
| 프롬프트 FM 3-90 적용 | BLUE/RED/WHITE 역할별 교리 지침 + MDMP 구조 | `prompts.py` |

### Phase 2-2 상세: parser.py 6종 복원 로직 (Phase 2 핵심 성과)

초기 실험에서 fallback 비율 100% → 점진적 수정으로 ~14%로 감소. 아래 6가지 fix가 적용됨:

| Fix # | 문제 | 수정 내용 | 파일 |
|---|---|---|---|
| Fix-1 | non-HOLD + `target_hex=null` → 전체 플랜 거부 | HOLD/DEFEND로 강등 (단일 액션) | `parser.py: validate_actions()` |
| Fix-2 | HOLD + `target_hex≠null` → 오류 | target_hex 무시하고 계속 진행 | `parser.py: validate_actions()` |
| Fix-3 | `action_type="maneuver"` 등 LLM 혼용 표현 | 별칭 매핑: `maneuver→move`, `defend→hold`, `fire→support_by_fire` | `parser.py: _ACTION_TYPE_ALIASES` |
| Fix-4 | `posture="offense"` 등 LLM 혼용 표현 | 별칭 매핑: `observe→maneuver`, `offense→attack`, `recon→maneuver`, `support_by_fire→support` | `parser.py: _POSTURE_ALIASES` |
| Fix-5 | 적 유닛 ID (예: `red-flank`) 사용 또는 부분 이름 (예: `"assault"`) | 반대 팩션 ID 드롭, suffix 매칭 (`"assault"→"blue-assault"`) | `parser.py: _soft_match_unit_id()` |
| Fix-6 | 일부 유닛 명령 누락 | 누락된 유닛 자동으로 HOLD/DEFEND 삽입 | `parser.py: _fill_missing_units()` |

**수정 효과 비교**:

| 실험 | fallback 비율 | 개선 폭 |
|---|---|---|
| prompt_test_v1 (mlx sampler 버그) | 100% | — |
| prompt_test_v3 (max_tokens=512) | 75% | +25%p |
| batch_stability 초기 (Fix-1 전) | 58% | — |
| batch_stability 후 (Fix-1~4 적용) | ~20% | -38%p |
| Phase 4-1 s1 (Fix-1~6 전체) | ~14% | -44%p |

### Phase 3 (베이스라인 배치 실험) — 이행률 100% ✅

**실험 규모**: 5 시나리오 × Rule-vs-Rule × 50 seeds = **250 게임** 완료

| 지표 | 전체 평균 | 비고 |
|---|---|---|
| DCR (교리 준수율) | **0.804** | Heuristic White Cell 기준 |
| TRS (전술 합리성) | **3.548** | 1~5 척도 |
| Action Entropy | **1.620** | Rule 에이전트 다양성 |
| ESI (공격성 변동성) | **0.102** | 낮은 변동성 — Rule 에이전트의 일관된 행동 |
| 총 게임 수 | 250 | `runs/phase3/baseline/` |

> **비고**: Phase 3 베이스라인은 `--fog-preset baseline` (vr=8, idr=3) 조건으로 실행됨. LLM 실험은 `--fog-preset llm` (vr=5, idr=2) 사용 — 비교 시 주의 필요.

### Phase 4 (대규모 LLM 실험) — 이행률 진행중 ⏳

#### Phase 4-1: Qwen2.5-7B-Instruct-4bit (Mac M4, MLX)

**현재 상태** (2026-03-25 기준): **48/100 게임** 완료

| 시나리오 | 완료 게임 수 | Blue 승률 | 베이스라인 대비 | 상태 |
|---|---|---|---|---|
| S1 평지 조우전 | 20 | **~20%** | 베이스라인 6% 대비 **+14%p** ↑ | ✅ 완료 |
| S2 산악 방어진지 | 20 | **~0%** | 베이스라인 ~96% 대비 **-96%p** ↓ | ✅ 완료 (이상 발견) |
| S3 시가지 전투 | 8 | — | — | ⏳ 진행중 |
| S4 하천 도하 | 0 | — | — | ⏳ 대기 |
| S5 포위 돌파 | 0 | — | — | ⏳ 대기 |

**S2 이상 원인 분석**: `unit_id hallucination` 발생. S2 이상의 시나리오는 유닛 ID에 설명적 이름(예: `blue-assault`, `red-flank`)을 사용하는데, 모델이 적 팩션 ID를 명령하거나 존재하지 않는 ID를 사용함. Fix-5 (`_soft_match_unit_id`)가 일부 복원하지만 ID 자체를 잘못 생성하는 경우 여전히 발생.

→ **추가 fix 필요**: `state_to_text.py`의 상황 보고서에 "Valid unit_ids for your orders: [...]" 라인 추가로 모델에 명시적 힌트 제공 예정.

#### Phase 4-2: Mistral-7B-Instruct (Colab, vLLM)

**현재 상태**: Colab 실험 완료, 결과 로컬 전송 대기

- 실행 조건: Colab A100, vLLM backend, `--max-tokens 1024`, `--fog-preset llm`
- 결과 전송 방법: `zip -r colab_results.zip runs/phase4/` → Google Drive → 로컬 복사

#### Phase 4-3: Llama-3.1-8B-Instruct (Colab, vLLM)

**현재 상태**: Colab 실험 완료, 결과 로컬 전송 대기

#### Phase 4-4: 베이스라인 재실험 (LLM 조건 fog preset)

> Phase 3 베이스라인 (`fog-preset baseline`)과 Phase 4 LLM 실험 (`fog-preset llm`)의 fog 조건이 달라 직접 비교가 어려움. 동일한 fog 조건의 베이스라인 재실험 필요 여부 검토 중.

### Phase 5 (통계 분석 및 논문) — 이행률 0% ⏳

Phase 4 전 모델 결과 수집 후 진행 예정.

---

## 8. 연구계획서 대비 추가 구현된 사항

계획서에는 없었으나 구현 과정에서 추가된 유익한 기능들:

| 추가 항목 | 구현 위치 | 의의 |
|---|---|---|
| `flanking_maneuver_agent.py` | `script_agents/` | Script 에이전트 다양성 확대 |
| `HeuristicWhiteCellAgent` | `white_cell.py` | LLM 없이 재현 가능한 DCR 평가 |
| `tactical_risk_score()` | `metrics.py` | 계획서에 없는 추가 지표 |
| `chat_templates.py` | `agents/backends/` | 모델별 프롬프트 포맷 자동 처리 |
| `test_cli_fog_safety.py` | `tests/` | CLI fog 파라미터 오용 방지 |
| `compare_combat_logs.py` | `scripts/` | deterministic vs stochastic 비교 자동화 |
| `check_llm_stability.py` | `scripts/` | JSON 파싱 안정성 분석 자동화 |
| `fog-preset` CLI 옵션 | `run_single_game.py`, `run_batch.py` | `baseline`/`llm` 프리셋으로 fog 조건 일괄 관리 |
| `--max-tokens` CLI 옵션 | `run_single_game.py`, `run_batch.py` | JSON 중단 방지를 위한 토큰 수 조정 (권장: 1024 이상) |
| `colab_experiments.ipynb` | 루트 디렉토리 | Colab A100 환경 실험 노트북 (parser 검증 + Phase 4 실험 전 과정) |

---

## 9. 주요 리스크 및 권장 조치

### 🔴 높음 — 논문 기술 오류 가능성

**[R1] Lanchester 모형 명칭 오류**

코드 docstring에 "square-law-inspired"라고 기재되어 있으나 실제 구현은 선형법칙(Linear Law)입니다. 논문 3.2.2절에서 이를 제곱법칙으로 기술하면 심사자에게 즉각 지적될 수 있습니다.

→ **조치**: docstring 수정 + 논문에서 "선형 Lanchester 법칙의 1-step 이산 근사"로 기술.

**[R2] ESI 정의 불일치**

계획서의 ESI (공격행동 비율) ≠ 구현의 ESI (턴간 변동성). 논문에서 계획서 정의대로 분석할 경우 코드 결과와 불일치.

→ **조치**: 구현에 `offensive_action_ratio()` 함수 추가 또는 논문 정의를 구현에 맞게 수정.

### 🟡 중간 — 실험 품질 영향

**[R4] White Cell LLM vs Heuristic 선택 미결**

계획서는 LLM 기반 White Cell을 명시했으나, 구현에는 Heuristic도 병행 존재합니다. 어느 것을 논문 실험에 사용할지 결정이 필요합니다.

→ **조치**: Phase 4 본 실험에서는 LLM 기반 WhiteCellAgent 사용 + Heuristic 결과와 상관관계 검증(내부 검증용).

**[R7] Unit ID Hallucination (신규 발견, 2026-03-25)**

S2 이상의 시나리오에서 모델이 아군 유닛 ID 대신 적 팩션 ID (`red-flank` 등)를 명령하거나 존재하지 않는 ID를 생성하는 현상이 관측됨. 이로 인해 S2 Blue 승률이 베이스라인 ~96% 대비 ~0%로 급락.

```
# 관측된 문제 유형:
# 1. 적 팩션 명령: {"unit_id": "red-flank", ...}  → _soft_match_unit_id()로 드롭
# 2. 부분 이름: {"unit_id": "assault", ...}  → suffix 매칭으로 복원
# 3. 완전 오류: {"unit_id": "blue-1", ...} (존재하지 않는 ID)  → ActionParseError
```

→ **조치 A (코드)**: `state_to_text.py`의 상황 보고서에 "Valid unit_ids for your orders: [blue-assault, blue-support, ...]" 명시적 라인 추가.

→ **조치 B (프롬프트)**: `OUTPUT_CONTRACT`에 이미 "MUST be one of your Friendly Unit IDs" 지침 추가됨 (현재 적용).

→ **완전 해결**: 조치 A (state_to_text.py 수정) 미완료. Claude Code prompt 작성됨, 실행 대기.

**[R8] Fog-of-War 조건 불일치 (신규 발견, 2026-03-25)**

Phase 3 베이스라인은 `fog-preset baseline` (vr=8, idr=3)으로 실행됐으나, Phase 4 LLM 실험은 `fog-preset llm` (vr=5, idr=2)으로 실행됨. 두 조건의 관측 범위 차이가 성과 비교에 혼입 변수로 작용할 수 있음.

→ **권장 조치**: Phase 4 완료 후 `fog-preset llm` 조건의 베이스라인 50게임 추가 실행 (Phase 4-4), 또는 논문에서 조건 차이를 명시하고 보정 방법 기술.

### 🟢 낮음 — 개선 권장

**[R5] `_CONTACT_RANGE = 1` 하드코딩**

시뮬레이션 엔진에서 교전 범위가 상수로 고정되어 있어 시나리오별 다른 교전 거리를 설정할 수 없습니다. 현재 시나리오 설계 상 문제없으나 확장성 제한.

**[R6] s2·s4에서 Rule 에이전트 교전 빈도 낮음**

계획서의 "시나리오 검증 기준: 교착 빈도 < 20%"와 충돌 가능성이나, LLM 에이전트에서는 공격적 행동이 나타나고 있어 오히려 비교 효과를 강화하는 방향으로 활용 가능.

~~**[R3] 시스템 프롬프트 단순화**~~ → **해소됨** (2026-03-25)

FM 3-90 교리 4항(집중/기습/경계/기동) + MDMP 3단계(ASSESS/DEVELOP/DECIDE) + `OUTPUT_CONTRACT` unit_id 지침 모두 `prompts.py`에 구현 완료.

---

## 10. 종합 평가 및 다음 단계

### 종합 평가

현재 구현은 연구계획서의 핵심 구조를 **높은 충실도(97%)** 로 반영하고 있습니다. 엔진, 에이전트, 분석 시스템, 시나리오, 베이스라인 모두 계획대로 완성되었으며, Phase 1~3 실험도 계획 기준을 대부분 충족했습니다.

Phase 4 실험은 진행 중이며, Qwen 모델에서 S1 성과 개선(+14%p)이 확인되었으나 S2에서 unit_id hallucination으로 인한 급격한 성과 저하가 관측되었습니다. 이는 LLM의 구조적 취약점으로, 추가 코드 수정(`state_to_text.py`)이 필요합니다.

### 즉시 조치 권고 (Phase 4 완료 전)

1. `state_to_text.py`: 상황 보고서에 "Valid unit_ids for your orders: [...]" 라인 추가 → unit_id hallucination 완전 해소
2. Phase 4 Qwen s3~s5 실험 완료 (현재 진행중)
3. Colab 실험 결과 로컬 복사 (Mistral, Llama 결과)

### 논문 작성 전 필수 조치

1. `lanchester.py` docstring: "square-law-inspired" → "linear law" 수정
2. `metrics.py`에 `offensive_action_ratio()` 함수 추가 (계획서 ESI 정의 구현)
3. Phase 4-4 베이스라인 재실험 (fog-preset llm 조건) 또는 논문에서 조건 차이 명시

### 다음 실험 단계 (우선순위 순)

```
1. [진행중] Phase 4 Qwen s3~s5 완료
2. [대기] Colab 결과 로컬 복사 및 검토
3. [대기] state_to_text.py unit_id 힌트 추가 후 S2~S5 재실험 검토
4. [대기] Phase 5 통계 분석 (Kruskal-Wallis, Mann-Whitney U)
5. [대기] 논문 작성 (Phase 5 결과 기반)
```

---

*검토 기준: `Research_plan.md` + `src/wargame/` 전체 코드 + Phase 1~4 실험 로그*
*최초 작성: Claude Cowork (2026-03-22) | 최종 업데이트: 2026-03-25*

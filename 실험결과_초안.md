# 4. 실험 설계 및 결과 분석

본 장에서는 3장에서 기술한 프레임워크에 기반하여 수행한 실험의 설계와 결과를 제시한다. 실험은 7B급 양자화 LLM 에이전트의 전술적 의사결정 능력을 (i) 교리 준수, (ii) 행동 다양성, (iii) 전투 효과성, (iv) 출력 안정성의 4가지 측면에서 정량적으로 평가하는 데 목적이 있다.

## 4.1 실험 환경 및 구현

시뮬레이션 프레임워크는 Python 3.11 기반으로 구현하였으며, LLM 추론은 Apple Silicon(M4) 환경에서 MLX 프레임워크를 통해 단일 노드에서 수행되었다. 이는 외부 API 호출 없이 폐쇄망 호환 추론이 가능함을 확인하기 위함이다. 청군 LLM 에이전트의 기반 모델은 Qwen2.5-7B-Instruct의 4비트 양자화 버전이며, 추론 매개변수는 temperature 0.7, max_tokens 1024, context window 2048로 설정하였다. 전투 모델은 Lanchester 선형 법칙을 적용하였고($\alpha = \beta = 0.05$, 잡음 표준편차 $\sigma = 0.1$), 전장안개는 이중 반경(가시 반경/식별 반경) 구조를 채택하되, 3.2.2절에서 기술한 바와 같이 규칙 기반 에이전트에는 (8, 3)을, LLM 에이전트에는 (5, 2)를 차등 적용하였다. 이는 사전 실험에서 규칙 기반 에이전트의 기본 시야로는 초기 부대 간 거리에서 교전이 발생하지 않아 비교 실험이 성립하지 않는 문제를 해결하기 위한 것이다. 각 게임은 최대 12턴으로 제한되며, 일방의 전 부대 격멸 시 조기 종료된다.

## 4.2 실험 설계 및 베이스라인

LLM 에이전트의 성능을 객관적으로 평가하기 위해 결정론적 규칙 기반 에이전트(Rule-based Agent)를 베이스라인으로 설정하였다. 규칙 기반 에이전트는 전력비, 적과의 거리, 가시 정보를 입력으로 하여 사전에 정의된 조건-행동(If-Then) 규칙에 따라 행동을 결정한다. 실험은 두 단계로 구성하였다. **Phase 3(Baseline)**에서는 청·홍 양 진영 모두에 규칙 기반 에이전트를 배치하여 5개 시나리오 × 50개 시드 = 총 250회 시뮬레이션을 수행하였다. **Phase 4(LLM)**에서는 청군에 Qwen2.5-7B LLM 에이전트, 홍군에 동일한 규칙 기반 에이전트를 배치하여 5개 시나리오 × 20개 시드 = 총 100회 시뮬레이션을 수행하였다. 두 단계 간 홍군 에이전트 및 시나리오 구성을 통제함으로써, 관찰되는 행동 차이를 청군 에이전트의 의사결정 메커니즘 차이에 귀속시킬 수 있도록 하였다.

평가 지표는 3.4.2절에서 정의한 (i) 승률, (ii) 교리 준수율(DCR), (iii) 행동 엔트로피, (iv) 폴백률, (v) 평균 종결 턴 외에 (vi) **공격 성향 지수(Engagement Stability Index, ESI)** 를 추가로 산출한다. 본 연구에서 ESI는 단순한 공격 행동 비율이 아니라, 게임 내 턴별 공격성 점수(offensive intensity score)의 **턴 간 평균 절대 변화(mean absolute change)** 로 정의되며, 값이 클수록 공격↔수세 간 전술 전환이 잦음을, 값이 작을수록 일관된 전술 자세를 유지함을 의미한다. 단순 공격 행동 비율(N_off / N_tot)은 보조 지표로 별도 산출한다.

통계적 유의성 검증에는 (i) 교리 준수율에 대한 단일 표본 t-검정(기준값 0.5), (ii) 행동 엔트로피의 분포 비교를 위한 Mann-Whitney U 검정, (iii) 승률 비교를 위한 Fisher 정확 검정을 적용하였다. 모든 검정의 유의 수준은 $\alpha = 0.05$로 설정하였다.

## 4.3 교리 준수율 분석

Qwen2.5-7B LLM 에이전트의 교리 준수율(DCR)은 100회 시뮬레이션 전체 평균 **0.718** (95% CI: [0.699, 0.738])을 기록하였다. 우연 수준(0.5)에 대한 단일 표본 t-검정 결과 $t = 22.45$, $p = 1.23 \times 10^{-40}$으로 통계적으로 매우 유의미하였으며, Cohen의 $d = 2.257$로 매우 큰 효과 크기를 나타냈다. 시나리오별로는 S2(산악, 0.761)와 S5(돌파, 0.758)에서 가장 높은 준수율이 관찰된 반면, S3(도시, 0.683)에서 상대적으로 낮은 값이 나타났다. 이는 시야가 제약되고 표적 식별이 어려운 시가지 환경에서 LLM이 일부 교리 원칙(예: 식별 후 교전, 측면 경계)을 일관되게 적용하지 못하는 경향을 시사한다.

규칙 기반 에이전트의 평균 DCR은 0.644로, LLM 에이전트가 절대값 기준 약 7.4%p, 상대 비율 기준 약 11.6% 높은 준수율을 달성하였다. 이러한 결과는 FM 3-90 교리에 기반한 시스템 프롬프트와 ASSESS-DEVELOP-DECIDE 3단계 추론 구조가 LLM의 의사결정을 교리에 부합하는 방향으로 효과적으로 유도하고 있음을 보여준다.

## 4.4 행동 다양성 분석

행동 다양성을 측정하는 행동 엔트로피(Action Entropy)의 게임당 평균은 LLM 에이전트가 **1.701 bits** (표준편차 0.204), 규칙 기반 에이전트가 **1.208 bits** (표준편차 0.325)로 산출되어, LLM 에이전트가 약 40.8% 높은 행동 다양성을 보였다. Mann-Whitney U 검정 결과 $U = 22{,}316$, $p = 8.48 \times 10^{-30}$으로 통계적으로 매우 유의미하였으며, 순위 이연 상관 $|r| = 0.785$의 큰 효과 크기를 나타냈다.

행동 유형의 분포를 비교하면, 규칙 기반 에이전트는 move(60.5%), hold(28.1%), withdraw(7.6%), attack(3.8%)의 4종 행동만을 사용한 반면, LLM 에이전트는 move(49.1%), hold(27.9%), support_by_fire(11.7%), attack(9.6%), recon(1.7%)의 5종 행동을 활용하였다. 특히 규칙 기반 에이전트의 룰셋에는 명시되지 않은 **지원사격(support_by_fire, 11.7%)** 과 **정찰(recon, 1.7%)** 행동을 LLM이 자발적으로 선택한 점은, 사전 정의된 규칙 집합을 넘는 창발적 전술 행동이 발현되었음을 시사한다. 다만 LLM 에이전트는 철수(withdraw) 행동을 한 차례도 선택하지 않았는데, 이는 공세 지향적 교리(FM 3-90 공격 편) 프롬프트의 영향으로 해석된다.

## 4.5 전투 결과 분석

시나리오별 청군 승률을 비교하면, S1(평지)에서는 LLM 25% vs 규칙 기반 18%로 LLM이 7%p 우세하였고, S5(돌파)에서는 LLM 45% vs 규칙 기반 36%로 9%p 향상이 관찰되었다. S4(하천)에서는 LLM 27% vs 규칙 기반 24%로 유사한 수준이었다. 반면 S3(도시)에서는 LLM 15% vs 규칙 기반 46%로 LLM이 31%p 낮은 승률을 기록하였으며, S2(산악)에서는 양측 모두 낮은 승률(LLM 6%, 규칙 기반 8%)과 높은 무승부 비율(각각 70%, 76%)을 보였다. 이는 산악 지형의 방어 수정치(1.5)가 양측의 공세 행동 효과를 제한함을 의미한다.

전체 100회 실험에 대한 청군 승률은 LLM 22.0%, 규칙 기반 26.4%로, Fisher 정확 검정 결과 차이는 통계적으로 유의하지 않았다(Odds Ratio = 0.786, $p = 0.417$). 이는 교리 준수율과 행동 다양성에서의 우위가 단기 전투 승률의 직접적 향상으로 이어지지는 않음을 의미한다. 한편 무승부 비율은 LLM 17.0%로 규칙 기반 34.4% 대비 절반 수준으로 감소하였는데, 이는 LLM 에이전트의 적극적 교전 추구 경향이 보다 결정적인 전투 결과로 이어지고 있음을 시사한다. 동시에 홍군 승률이 LLM 실험에서 61.0% (규칙 기반 39.2%)로 증가한 점은, LLM 에이전트의 공세 지향성이 규칙 기반 홍군의 결정론적 방어에 일부 취약점을 노출하였음을 보여주는 결과로 해석된다.

## 4.6 출력 안정성 및 폴백 분석

소형 LLM의 구조화된 출력 안정성을 측정한 결과, 전체 100회 시뮬레이션에서 다층 오류 복구의 결정론적 폴백이 적용된 행동의 비율(폴백률)은 평균 **12.12%** 였다. 시나리오별로는 S1(15.1%)에서 가장 높고 S5(9.7%)에서 가장 낮게 분포하였다. 이 수치는 4비트 양자화된 7B 모델이 여전히 일정 비율의 JSON 스키마 위반을 발생시키지만, 3.3.3절에서 기술한 다층적 오류 복구(스키마 검증 → 정규화 재파싱 → 소프트 매칭 → 의미 검증 → 폴백) 구조를 통해 모든 시뮬레이션이 중단 없이 완주되었음을 의미한다. 즉, 본 프레임워크는 소형 LLM의 출력 불안정성이라는 본질적 한계를 엔지니어링 수준에서 흡수하면서도, 폴백률 자체를 모델 출력 신뢰성의 부가 지표로 활용할 수 있음을 보였다.

## 4.7 정성 분석

LLM 에이전트의 의사결정 로그(reasoning 필드)를 정성적으로 분석한 결과, 다음과 같은 행동 패턴이 반복적으로 관찰되었다. 첫째, LLM 에이전트는 행동 근거를 제시할 때 교리 원칙을 명시적으로 참조하는 경향을 보였다. 예를 들어 "집중의 원칙에 따라 주공 방향에 전투력을 집중", "경계의 원칙에 따라 측면에 정찰 부대를 배치"와 같은 표현이 빈번히 등장하였다. 둘째, 지원사격(support_by_fire)의 자발적 활용은 단순한 이동-공격 패턴을 넘어 화력 지원과 기동을 결합한 복합 전술의 시도로 해석할 수 있다. 셋째, 일부 시나리오(특히 S3)에서 시야가 제한된 상황에 직면했을 때 정찰 행동을 선행한 후 교전을 결정하는 사례가 관찰되어, ASSESS 단계에서의 상황 평가가 후속 행동에 반영되는 양상을 확인하였다. 다만 동일 부대에 대해 인접 턴 간 일관성이 부족한 사례도 존재하였으며, 이는 단기 메모리 구조의 부재와 관련된 것으로 보인다.

## 4.8 종합 결과 요약

본 장의 실험 결과를 종합하면 다음과 같다. 7B급 4비트 양자화 LLM 에이전트는 (i) 교리 준수율 0.718로 우연 수준 및 규칙 기반 에이전트를 모두 통계적으로 유의미하게 상회하였고($p < 10^{-40}$), (ii) 행동 엔트로피 1.701 bits로 규칙 기반 에이전트 대비 40.8% 높은 행동 다양성을 보였으며($p < 10^{-29}$), (iii) 규칙에 정의되지 않은 지원사격·정찰 행동을 자발적으로 발현하였고, (iv) 폴백률 12.12% 수준의 출력 불안정성에도 불구하고 다층 오류 복구 구조를 통해 모든 시뮬레이션이 안정적으로 완주되었다. 그러나 (v) 청군 승률은 22.0%로 규칙 기반 26.4%와 통계적으로 구분되지 않아($p = 0.417$), 교리 준수와 행동 다양성의 향상이 단순 승률 지표로는 즉각적으로 환원되지 않음을 확인하였다. 이러한 결과의 해석과 시사점은 5장에서 보다 상세히 논의한다.

---

※ 본 장은 신규 인용을 추가하지 않으며, 3장까지의 인용 번호([1]–[24])를 그대로 따른다. 모든 수치는 Phase 3(규칙 기반 250회)·Phase 4(LLM 100회) 실험 로그(JSONL)의 후처리 분석 결과에 근거한다.

# AI 도구 역할 분담 가이드 — Multi-Agent Wargame 연구

> **작성일**: 2026-03-22
>
> 이 가이드는 GPT Codex, Claude Code, Claude Cowork 세 도구의 역할을 명확히 구분하고,
> 각 단계에서 어떤 도구에 어떤 프롬프트를 사용해야 하는지 정리한 실용 매뉴얼입니다.

---

## 도구별 핵심 역할 요약

| 도구 | 실행 환경 | 핵심 강점 | 이 프로젝트에서의 역할 |
|---|---|---|---|
| **GPT Codex** | 터미널 / 웹 | 새 코드 생성, 대규모 리팩토링 | 새 기능 구현 · 모듈 신규 작성 |
| **Claude Code** | 터미널 CLI | 다중 파일 컨텍스트 이해, 테스트·실행 | 코드 수정·디버깅·검증 실행 |
| **Claude Cowork** | 데스크탑 앱 | 문서 작성, 계획·조율, 연구 종합 | 진행 관리·보고서·결과 해석 |

---

## 1. GPT Codex — 새 기능 구현 전문

### 적합한 작업

- 새로운 Python 모듈/클래스를 **처음부터** 작성
- 기존 모듈의 대규모 리팩토링 (인터페이스 변경 등)
- 복잡한 알고리즘 구현 (새 전술 스크립트, 새 분석 메트릭)
- 테스트 코드 일괄 생성

### Phase별 활용 시점

| Phase | 작업 | Codex에게 요청할 타이밍 |
|---|---|---|
| Phase 3 | 새 Script 에이전트 추가 | `flanking_maneuver`, `delay_defense` 전술 로직 구현 시 |
| Phase 4 | vLLM 백엔드 연동 | `VLLMBackend` 클래스 신규 작성 시 |
| Phase 5 | 새 분석 메트릭 추가 | `metrics.py`에 새 지표 함수 추가 시 |

---

### GPT Codex 프롬프트 모음

#### [Codex-01] 새 Script 에이전트 구현

```
You are implementing a new scripted military agent for a wargame simulation.

Project structure:
- src/wargame/agents/script_agents/ — existing script agents
- Each agent implements: decide(state: FactionViewState) -> ActionCommand

Task: Implement a new script agent called `FlankingManeuverAgent` that:
1. Moves units toward the enemy's flank (perpendicular to direct approach)
2. Attacks if adjacent to enemy
3. Falls back to HOLD if no enemies are visible

Follow the same interface as the existing `FrontalAssaultAgent` in the same directory.
Write the complete Python file including docstrings and type hints.
```

---

#### [Codex-02] vLLM 백엔드 클래스 신규 작성

```
Implement a vLLM inference backend class for a multi-agent wargame LLM integration.

Existing MLX backend reference: src/wargame/agents/local_llm.py (MLXLocalLLMBackend class)

Task: Write a new class `VLLMLocalLLMBackend` that:
1. Accepts model_id as constructor arg
2. Implements generate(prompt: str, max_tokens: int) -> str
3. Uses vllm.LLM and SamplingParams internally
4. Handles exceptions and returns empty string on failure

Use the same interface as MLXLocalLLMBackend so they are drop-in replacements.
```

---

#### [Codex-03] 새 분석 메트릭 함수 추가

```
Add a new analysis metric function to a Python metrics module.

Existing file: src/wargame/analysis/metrics.py
Existing functions for reference: action_entropy(log_path), escalation_sensitivity_index(log_path)

Task: Implement `tactical_risk_score(log_path: Path) -> float` that:
1. Reads a JSONL game log
2. For each turn, calculates a risk score based on: force ratio, proximity to enemy, terrain type
3. Returns the mean risk score across all turns
4. Returns 0.0 if no valid data found

Follow the same pattern as existing metric functions. Include type hints and docstring.
```

---

#### [Codex-04] 테스트 코드 일괄 생성

```
Generate pytest test cases for the following functions in src/wargame/analysis/metrics.py:
- action_entropy(log_path: Path) -> float
- escalation_sensitivity_index(log_path: Path) -> float
- doctrine_compliance_rate(log_path: Path) -> float

Requirements:
- Use pytest fixtures to create temporary JSONL log files
- Test normal case, edge case (empty log), and error case (malformed JSON)
- Each function should have at least 3 test cases
- Follow the naming convention in existing tests/test_lanchester.py
```

---

## 2. Claude Code — 코드 수정·디버깅·실행 전문

### 적합한 작업

- 기존 파일의 **버그 수정** (특정 줄 수정, 함수 패치)
- 여러 파일에 걸친 인터페이스 변경 추적 및 일관성 수정
- `pytest` 실행 결과 해석 및 실패 원인 수정
- 실험 스크립트 실행 및 출력 로그 분석
- Git 관리 (커밋, 브랜치, diff 검토)

### Phase별 활용 시점

| Phase | 작업 | Claude Code에게 요청할 타이밍 |
|---|---|---|
| Phase 1 | 1-4 비교 결과 해석 | stochastic 비교 스크립트 실행 후 결과 분석 |
| Phase 2 | JSON 파싱 오류 디버깅 | LLM 출력 파싱 실패 원인 추적 시 |
| Phase 3 | 단위 테스트 실패 수정 | `pytest test_baseline_agents.py` 실패 시 |
| Phase 4 | 배치 실행 오류 대응 | `run_batch.py` 런타임 오류 발생 시 |

---

### Claude Code 프롬프트 모음

#### [Code-01] 단위 테스트 실패 디버깅

```
Run the following test and fix any failures:

  pytest tests/test_baseline_agents.py -v

After running:
1. Show me the full failure output
2. Identify the root cause in the source code
3. Apply the minimal fix to make the tests pass
4. Re-run to confirm all tests pass

Do not change test logic — fix the implementation, not the tests.
```

---

#### [Code-02] LLM JSON 파싱 오류 원인 추적

```
I'm seeing high fallback rates (>10%) when running the LLM agent.
The relevant code is in src/wargame/agents/local_llm.py

Tasks:
1. Read the extract_json_object() function and the LLM agent's action parsing logic
2. Find edge cases that would cause parsing to fail silently and trigger fallback
3. Add a debug logging line that prints the raw LLM output when parsing fails
4. Suggest (but don't apply) a more robust parsing fix
```

---

#### [Code-03] run_batch.py 런타임 오류 수정

```
I got this error while running run_batch.py:

[PASTE ERROR OUTPUT HERE]

Tasks:
1. Read scripts/run_batch.py and identify the cause
2. Check if any related source files (src/wargame/) also need changes
3. Apply the fix
4. Verify by running:
   python scripts/run_batch.py --scenario s1_open_encounter --matchup rule,rule --seed-count 2 --visibility-radius 8 --identification-radius 3 --output-dir runs/test_fix/
```

---

#### [Code-04] 여러 파일에 걸친 인터페이스 변경 추적

```
I need to add a new field `terrain_bonus: float` to the ActionCommand dataclass
in src/wargame/core/action.py

Tasks:
1. Add the field with default value 0.0
2. Search all files that construct or consume ActionCommand and update them
3. Run pytest to confirm no regressions
4. Show me a summary of all files changed
```

---

#### [Code-05] 실험 결과 빠른 요약

```
Run this analysis script and summarize the results:

python -c "
import json, pathlib
for label, path in [
    ('deterministic', 'runs/phase1/s1_deterministic_vr8_idr3.jsonl'),
    ('stochastic',    'runs/phase1/s1_stochastic_vr8_idr3.jsonl'),
]:
    records = [json.loads(l) for l in pathlib.Path(path).read_text().strip().split('\n') if l]
    last = records[-1]
    units = last.get('state', {}).get('units', {})
    blue = sum(u['strength'] for u in units.values() if u.get('faction') == 'blue')
    red  = sum(u['strength'] for u in units.values() if u.get('faction') == 'red')
    combat_turns = sum(1 for r in records if r.get('combat') and r['combat'].get('casualties_by_unit'))
    print(f'{label}: Blue={blue}, Red={red}, 교전={combat_turns}턴')
"

Tell me:
1. Whether the stochastic result differs from the deterministic result (expected)
2. Whether the noise effect is within a reasonable range (±20% of deterministic losses)
```

---

## 3. Claude Cowork (이 세션) — 계획·문서·결과 종합 전문

### 적합한 작업

- `todo_list.md`, `검토_보고서.md` 등 **문서 작성 및 업데이트**
- 실험 결과 데이터를 논문 수준으로 **해석 및 정리**
- 연구 계획 검토 및 다음 단계 결정
- Phase 완료 여부 판단 및 진행 조율
- 웹 검색이 필요한 기술 조사 (모델 비교, 논문 참조)
- 최종 논문 초안 작성

### Phase별 활용 시점

| Phase | 작업 | Cowork에게 요청할 타이밍 |
|---|---|---|
| 전체 | 진행 상황 정리 | 각 Phase 완료 후 todo_list 업데이트 |
| Phase 1 완료 | 결과 표 정리 | 5개 시나리오 수치를 보고서 형식으로 정리 |
| Phase 4 완료 | 논문 핵심 지표 표 작성 | 3개 LLM × 5개 시나리오 결과 취합 |
| Phase 5 | 논문 초안 섹션 작성 | 통계 결과를 논문 텍스트로 변환 |

---

### Claude Cowork 프롬프트 모음

#### [Cowork-01] Phase 완료 보고 및 todo 업데이트

```
Phase 1이 완료됐습니다. 아래 결과를 todo_list.md에 반영해주세요.

실행 결과:
- 결정론적: Blue=292, Red=297, 교전=1턴
- 확률적: Blue=289, Red=301, 교전=1턴

확인 사항:
1. 두 결과의 병력 수치가 다름 → stochastic noise 정상 동작 확인
2. Phase 1 전체 완료로 체크
3. 다음 단계(Phase 2-1 모델 다운로드)를 "지금 당장 해야 할 작업" 1순위로 갱신해주세요.
```

---

#### [Cowork-02] 실험 결과 논문용 표 작성

```
아래 Phase 4 실험 결과를 논문 Table 2 형식으로 정리해주세요.

[결과 데이터 붙여넣기]

요구사항:
- 에이전트: Script / Rule / Random / Qwen2.5-7B / Mistral-7B / Llama-3.1-8B
- 지표: Blue 승률(%), Action Entropy(bits), DCR(%), ESI, JSON 파싱률(%)
- 평균 ± 표준편차 형식
- 통계적으로 유의한 차이는 * 표시 (p<0.05)
- Markdown 표 형식으로 출력
```

---

#### [Cowork-03] 통계 결과 해석 및 논문 텍스트 변환

```
아래 통계 검정 결과를 논문 Results 섹션 텍스트로 작성해주세요.

RQ1 결과:
- Qwen2.5-7B: n=500, mean DCR=0.71, t=8.34, p<0.001, Cohen's d=0.58
- Mistral-7B: n=500, mean DCR=0.63, t=4.21, p<0.001, Cohen's d=0.37
- Llama-3.1-8B: n=500, mean DCR=0.68, t=6.15, p<0.001, Cohen's d=0.48

요구사항:
- 학술 논문 스타일 (3인칭, 수동태)
- APA 통계 표기법 사용
- 영어로 작성
- 3~4 문단 분량
```

---

#### [Cowork-04] 코드 검토 보고서 업데이트

```
새로 추가된 코드를 검토하고 검토_보고서.md를 업데이트해주세요.

추가된 파일/변경 사항:
- scripts/run_batch.py: --visibility-radius, --identification-radius 인자 추가
- src/wargame/analysis/plots.py: matplotlib 시각화 함수 5개 추가

검토 관점:
1. 기존 인터페이스와의 일관성
2. 엣지 케이스 처리 여부
3. 테스트 커버리지 충분성
```

---

#### [Cowork-05] 다음 단계 우선순위 결정

```
현재 todo_list.md를 읽고 다음 상황을 고려해서 이번 주 실행 우선순위를 정해주세요.

현재 상황:
- Mac M4에서 실행 가능 시간: 하루 약 4시간
- Colab Pro 크레딧: 50시간 남음
- 마감: 4주 후 논문 제출

고려사항:
- Phase 2 모델 다운로드 시간 (4~5GB, 약 30분)
- Phase 3 배치 실험 예상 시간 (3종 × 5시나리오 × 50회)
- Phase 4 LLM 실험 예상 시간 (각 모델 약 10시간)

이번 주 최적 실행 순서와 각 작업 예상 소요 시간을 알려주세요.
```

---

## 도구 선택 빠른 판단 기준

```
새 파일/클래스를 처음 만들어야 하는가?
  → YES: GPT Codex

기존 코드의 버그를 수정하거나 테스트를 실행해야 하는가?
  → YES: Claude Code

실험 결과를 해석하거나, 문서를 작성하거나, 다음 계획을 세워야 하는가?
  → YES: Claude Cowork
```

---

## 협업 워크플로우 예시 — Phase 3 베이스라인 실험

```
1. [Cowork] "Phase 3 실험 순서와 예상 소요 시간을 정리해줘"
      ↓ 계획 수립
2. [Codex]  "ScriptAgent flanking_maneuver 전술 로직을 새로 구현해줘"
      ↓ 코드 생성
3. [Code]   "새 스크립트 에이전트 단위 테스트 실행하고 실패 수정해줘"
      ↓ 테스트 통과
4. [Code]   "Phase 3 배치 실험 (3종×5시나리오×50회) 실행해줘"
      ↓ 실험 데이터 생성
5. [Cowork] "Phase 3 실험 결과를 논문 Table 1 형식으로 정리해줘"
      ↓ 보고서 작성
6. [Cowork] "todo_list.md Phase 3 완료 체크 및 Phase 4 준비사항 업데이트해줘"
```

---

*이 가이드는 실험 진행에 따라 지속 업데이트 예정*

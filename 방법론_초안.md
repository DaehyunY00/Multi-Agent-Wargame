# 3. 방법론

본 장에서는 7B급 소형 LLM을 활용한 다중 에이전트 워게임 시뮬레이션 프레임워크의 설계 및 구현을 기술한다. 본 프레임워크는 (1) 헥사곤 격자 기반 전술 환경, (2) Lanchester 전투 모델 기반 교전 해석, (3) 군사의사결정과정(MDMP) 구조의 LLM 청군(Blue) 에이전트, (4) 규칙 기반 홍군(Red) 에이전트, (5) 교리 준수를 평가하는 White Cell 에이전트, (6) 실험 실행 및 로그 수집 모듈로 구성된다.

## 3.1 전체 아키텍처

본 프레임워크는 턴 기반(turn-based) 폐쇄루프 구조로 설계되었으며, 시뮬레이션 코어, 에이전트 계층, 평가 계층, 실행 및 로깅 계층의 4개 모듈로 분리된다. 각 턴은 (i) 상태 관측 및 시야 필터링, (ii) 청군 LLM 에이전트의 행동 계획 생성, (iii) 홍군 규칙 기반 에이전트의 행동 결정, (iv) 행동 검증 및 충돌 해소, (v) Lanchester 모델 기반 교전 판정, (vi) 상태 갱신 및 White Cell 평가, (vii) 턴 로그 기록의 7단계로 진행된다. 각 모듈은 명시적 인터페이스로 분리되어 있어, 향후 다른 LLM 백엔드(MLX, vLLM 등)나 전투 모델로의 교체가 가능하도록 설계되었다.

전술 환경은 20×15 크기의 헥사곤 격자(axial 좌표계)로 구성되며, 헥사곤 격자는 6방향 균등 이동이 가능하여 정사각 격자 대비 방향 편향이 적다는 점에서 워게임 공간 표현의 표준 방식으로 채택되었다 [19]. 각 헥스 셀은 평지, 산악, 도시, 하천, 도하점 등의 지형 속성을 가지며, 지형은 이동 비용과 방어 수정치(defense modifier)에 영향을 준다. 청군과 홍군은 각 3개의 부대(unit)를 보유하며, 각 부대는 초기 전력(strength) 90~110 사이의 값으로 시작한다. 시뮬레이션은 임무 종결 조건(어느 한 측의 전 부대 격멸, 목표 점령, 또는 최대 12턴 도달) 시 종료된다.

## 3.2 전투 시뮬레이션 설계

### 3.2.1 Lanchester 전투 모델

본 연구에서는 결정론적·재현 가능한 전투 결과 산출을 위해 Lanchester 선형 법칙(Linear Law)의 1단계 이산 근사(one-step discrete approximation)를 채택하였다. 두 부대 간 단일 교전에서 청군 손실 $\Delta B$와 홍군 손실 $\Delta R$은 다음과 같이 산출된다.

$$\Delta B = \frac{\beta \cdot R \cdot \Delta t}{m_B}, \quad \Delta R = \frac{\alpha \cdot B \cdot \Delta t}{m_R}$$

여기서 $B$, $R$은 각각 청군·홍군의 현재 전력, $\alpha$, $\beta$는 양측의 소모율(attrition rate), $\Delta t$는 단위 시간 간격, $m_B$, $m_R$은 지형 및 자세에 따른 방어 수정치이다. 본 연구의 기준 설정에서는 $\alpha = \beta = 0.05$, $\Delta t = 1.0$으로 두었으며, 재현성을 위해 결정론적 모드를 기본으로 사용하되 민감도 분석을 위한 확률적 모드(가우시안 잡음 $\sigma=0.1$)도 함께 구현하였다. 산출된 손실값은 비음수 제약 및 보존 법칙(전체 손실이 가용 전력을 초과하지 않음)을 만족하도록 후처리된다.

Lanchester 모델은 가장 대표적인 전투 소모 모델 중 하나로, 워게임 및 강화학습 기반 전투 시뮬레이션에서 폭넓게 활용되어 왔다 [20]. 본 프레임워크는 LLM 에이전트의 의사결정 결과(이동·교전 행동)를 Lanchester 모델에 입력으로 전달함으로써, LLM의 자연어 기반 추론과 수학적 전투 모델의 물리적 타당성을 결합하는 하이브리드 구조를 형성한다.

### 3.2.2 시야 및 정보 비대칭

전장 정보의 부분 관측성(partial observability)을 모사하기 위해 이중 반경(dual-radius) 전장 안개(Fog of War) 모델을 도입하였다. 각 부대는 가시 반경(visibility radius)과 식별 반경(identification radius)의 두 가지 시야 매개변수를 가지며, 이에 따라 적 부대는 식별(IDENTIFIED), 탐지(DETECTED), 은닉(HIDDEN)의 3단계 가시 수준으로 분류된다. 식별 단계에서는 부대 종류·전력·상태 등 상세 정보가 제공되고, 탐지 단계에서는 위치만 제공되며, 은닉 단계에서는 정보가 제공되지 않는다. 본 연구에서는 에이전트 유형별 정보 처리 특성을 반영하기 위해 두 가지 시야 구성을 차등 적용하였다. 즉, 결정론적 규칙 기반 에이전트에는 (가시 반경 8, 식별 반경 3)을, LLM 에이전트에는 (가시 반경 5, 식별 반경 2)를 적용한다. 이러한 차등은 사전 실험 결과, 규칙 기반 에이전트의 기본 시야로는 초기 부대 간 거리(6~7 헥스)에서 교전 자체가 발생하지 않아 비교 실험이 불가능했던 반면, LLM 에이전트는 자연어 추론을 통해 보다 좁은 시야에서도 의미 있는 행동을 생성할 수 있음을 확인한 데 따른 것이다. 본 비대칭 조건이 결과 해석에 미치는 영향과 그 한계는 5.4절에서 별도로 논의한다.

### 3.2.3 시나리오 설계

본 연구에서는 다양한 전술 상황에서의 LLM 에이전트 성능을 평가하기 위해 5개의 표준 시나리오를 설계하였다. (S1) 평지 조우전(Open Encounter)은 지형 영향이 최소화된 환경에서의 조우전을, (S2) 산악 공격(Mountain Assault)은 고지대 점령 작전을, (S3) 도시 전투(Urban Fight)는 시가지 환경에서의 근접전을, (S4) 하천 도하(River Crossing)는 도하점을 둘러싼 공방전을, (S5) 돌파 작전(Breakout)은 적 방어선 돌파 임무를 모사한다. 각 시나리오는 동일한 20×15 격자, 양측 3개 부대, 최대 12턴이라는 공통 골격을 유지하면서, 지형 배치, 초기 부대 위치, 임무 목표만을 차별화하였다. 이는 시나리오 간 비교 가능성을 확보하면서도 다양한 전술 맥락에서의 의사결정 특성을 분석할 수 있도록 하기 위함이다.

## 3.3 LLM 에이전트 설계

### 3.3.1 모델 선정 및 양자화

본 연구에서는 청군 에이전트의 기반 모델로 Qwen2.5-7B-Instruct를 선정하였다 [21]. Qwen2.5는 알리바바 그룹이 공개한 오픈소스 LLM 계열로, 7B 파라미터 규모에서도 명령 수행, 구조화된 출력 생성, 다국어 추론에서 경쟁력 있는 성능을 보고하고 있다. 본 연구의 핵심 제약 조건이 폐쇄망 환경에서의 단일 노드 운용이라는 점을 고려하여, 4비트 가중치 양자화(weight-only quantization)를 적용한 변형을 채택하였다. 4비트 양자화를 통해 모델의 메모리 점유는 약 4~5GB 수준으로 감소하며, 이는 단일 GPU 또는 Apple Silicon 통합 메모리 환경에서의 실시간 추론을 가능하게 한다. 모델 추론 백엔드는 Apple Silicon 환경에서의 효율적 추론을 지원하는 MLX 프레임워크를 기본으로 사용하되, 서버 환경 확장성을 위해 vLLM 백엔드도 함께 구현하였다.

### 3.3.2 MDMP 기반 추론 구조

LLM 청군 에이전트의 의사결정 과정은 미 육군 군사의사결정과정(MDMP)을 단순화·구조화하여, ASSESS(상황 평가) → DEVELOP(행동방침 개발) → DECIDE(결정 및 행동 선택)의 3단계로 구성하였다. 이러한 단계적 추론 구조는 사고의 연쇄(Chain-of-Thought) 프롬프팅이 중간 추론 단계를 명시함으로써 복잡한 추론 과제에서 성능을 향상시킨다는 선행 연구 [22]에 이론적 기반을 두며, 동시에 추론(reasoning)과 행동(action)을 교차 생성하는 ReAct 패러다임 [23]의 사고-행동-관찰 루프를 본 도메인에 맞게 적용한 것이다.

각 단계의 입력 컨텍스트는 (i) 시스템 프롬프트, (ii) 전장 상태 텍스트화 결과, (iii) 직전 턴 요약으로 구성된다. 시스템 프롬프트는 미 육군 야전교범 FM 3-90(공격·방어 전술)의 핵심 원칙(집중, 기습, 기동, 화력의 통합 등)을 자연어로 부호화하여 제공하며, 에이전트가 교리에 부합하는 전술적 추론을 수행하도록 유도한다. 전장 상태 텍스트화는 헥사곤 격자, 부대 위치, 전력, 시야, 지형 정보를 결정론적 규칙에 따라 자연어 문장으로 변환하는 모듈로, 동일 상태에 대해 항상 동일한 텍스트 표현이 생성되도록 하여 실험의 재현성을 보장한다.

### 3.3.3 구조화된 출력 및 다층적 오류 복구

LLM 에이전트의 출력은 사전 정의된 JSON 스키마에 따른 행동 계획(action plan)으로 강제된다. 각 부대에 대해 행동 유형(이동/공격/방어/대기), 대상 좌표 또는 표적 ID, 사유(rationale)를 명시하며, 스키마 준수 여부는 파서(parser) 모듈에서 검증된다. 그러나 소형 LLM은 구조화된 출력 생성에서 일정 비율의 오류를 발생시키며, 환각(hallucination) 또한 안전이 중시되는 군사 응용에서의 핵심 위험 요인으로 지적된다 [24]. 이를 완화하기 위해 본 프레임워크는 다층적 오류 복구(multi-layer error recovery) 구조를 도입하였다.

구체적으로, (i) 1차 파싱에서는 엄격한 JSON 스키마 검증을 수행하고, (ii) 실패 시 코드 펜스 제거, 후행 콤마 보정 등의 정규화 후 재파싱을 시도하며, (iii) 부대 ID가 부정확한 경우 편집 거리 기반 소프트 매칭(soft matching)으로 가장 유사한 부대 ID로 매핑하고, (iv) 행동의 의미적 검증(존재하지 않는 좌표, 사거리 초과, 식별되지 않은 표적 지정 등) 단계에서 위반 행동을 식별하며, (v) 위 모든 단계에서 복구가 실패한 부대에 대해서는 결정론적 폴백 계획(fallback plan, 기본 방어 자세)을 적용한다. 이러한 다층적 복구 구조는 단일 턴의 LLM 출력 오류가 시뮬레이션 전체의 중단을 야기하지 않도록 보장하며, 동시에 폴백 사용 빈도 자체가 LLM의 출력 안정성을 정량적으로 측정하는 부가 지표로 활용된다.

### 3.3.4 홍군 에이전트와 White Cell 에이전트

홍군(Red) 에이전트는 비교 기준선(baseline)으로서 결정론적 규칙 기반 에이전트로 구현되었다. 이 에이전트는 가장 가까운 식별된 적 부대를 향한 이동, 사거리 내 우선순위 표적 공격, 임계 전력 이하 시 후퇴 등의 단순 규칙 집합을 따른다. 동일한 규칙 기반 홍군과의 대결을 모든 LLM 실험에서 동일하게 적용함으로써, LLM 에이전트의 행동 차이가 모델 자체의 의사결정 특성에 기인함을 통제한다.

White Cell 에이전트는 워게임의 통제관(controller) 역할을 수행하는 별도의 평가 에이전트로, 매 턴 종료 시 (i) 양측 행동의 교리 준수 여부 평가, (ii) 임무 진척도 평가, (iii) 다음 턴 환경 변화(증원, 기상 등) 결정의 기능을 수행한다. 본 연구에서는 White Cell의 교리 평가 결과를 정량 지표인 교리 준수율(Doctrine Compliance Rate, DCR)의 산출 근거로 활용한다. DCR은 한 게임에서 청군이 수행한 전체 행동 중 교리에 부합하는 것으로 평가된 행동의 비율로 정의된다. 평가의 일관성을 확보하기 위해 White Cell의 평가 기준은 사전에 정의된 체크리스트(시야 확보 후 기동, 집중의 원칙 준수, 무모한 노출 회피 등)에 기반하여 결정론적으로 산출되도록 설계하였다.

## 3.4 실험 실행 및 로그 수집

### 3.4.1 실험 구성

본 연구의 실험은 5개 시나리오 × 20회 반복 = 총 100회의 시뮬레이션으로 구성된다. 각 반복은 사전 정의된 시드(seed) 집합에 따라 결정론적으로 생성되며, 동일한 시드에 대해 동일한 초기 상태와 동일한 LLM 디코딩(temperature=0)이 적용되도록 통제하였다. 각 시나리오 내에서는 동일한 LLM 에이전트(Qwen2.5-7B-Instruct 4비트 양자화)와 동일한 규칙 기반 홍군 에이전트가 대결하며, 시나리오 간에는 지형, 초기 부대 배치, 임무 목표만이 변화한다.

### 3.4.2 로그 수집 및 분석 지표

각 시뮬레이션에서 수집되는 로그는 JSON Lines(JSONL) 형식으로 턴 단위로 저장되며, 다음 항목을 포함한다. (i) 턴 번호 및 시각, (ii) 양측 부대의 위치·전력·상태, (iii) LLM 에이전트의 원시 출력, 파싱 결과, 폴백 사용 여부, (iv) Lanchester 모델의 입력·출력 및 손실 값, (v) White Cell의 평가 결과 및 DCR 산출 내역, (vi) 임무 종결 조건 충족 여부. JSONL 형식은 턴 단위의 점진적 추가가 용이하고, 외부 분석 도구와의 호환성이 높아 사후 분석에 적합하다.

LLM 에이전트의 행동 특성을 정량적으로 평가하기 위해 본 연구에서 채택한 핵심 지표는 다음과 같다. (i) 승률(Win Rate) 및 평균 잔존 전력은 전술적 효과성을 측정하며, (ii) 교리 준수율(DCR)은 White Cell 평가에 기반하여 행동이 교리에 부합하는 정도를 측정한다. (iii) 행동 엔트로피(Action Entropy)는 한 게임 내 청군이 선택한 행동 유형 분포의 섀넌 엔트로피로 정의되며, 에이전트의 행동 다양성을 측정한다. 엔트로피가 높을수록 다양한 행동 유형을 균형 있게 사용함을 의미하며, 낮을수록 특정 행동에 편중됨을 의미한다. (iv) 폴백 사용률(Fallback Rate)은 다층적 오류 복구에서 결정론적 폴백이 적용된 행동의 비율로, LLM의 구조화된 출력 안정성을 측정한다. (v) 평균 종결 턴(Average Termination Turn)은 의사결정의 결단성과 임무 진척 속도를 측정한다.

### 3.4.3 통계 검정

LLM 에이전트와 규칙 기반 에이전트의 행동 차이가 통계적으로 유의한지 검증하기 위해, 본 연구는 (i) 연속 변수(잔존 전력, 행동 엔트로피, DCR)에 대해서는 정규성 가정 하의 독립표본 t-검정과 정규성 위배 시 Mann-Whitney U 검정을, (ii) 이산 변수(승률 등 비율 변수)에 대해서는 Fisher 정확 검정을 적용한다. 다중 비교에 따른 1종 오류 누적을 통제하기 위해 Bonferroni 보정을 함께 적용한다. 모든 통계 검정의 유의 수준은 $\alpha = 0.05$로 설정한다.

### 3.4.4 재현성 확보

본 프레임워크는 실험의 완전한 재현성을 핵심 설계 원칙으로 한다. 이를 위해 (i) 모든 시나리오, 시드, 모델 설정, 디코딩 매개변수를 YAML 구성 파일로 외재화하였으며, (ii) LLM 디코딩을 결정론적 모드(temperature=0)로 고정하고, (iii) Lanchester 모델의 기본 모드를 결정론적 모드로 설정하였으며, (iv) 모든 턴 로그와 함께 사용된 모델 버전, 양자화 방식, 백엔드 라이브러리 버전을 메타데이터로 함께 기록한다. 이러한 설계는 동일한 환경에서 동일한 시드를 사용하는 경우 동일한 결과가 재현되도록 보장한다.

---

## 추가 참고문헌 (3장 추가)

[19] A. Patel, "Hexagonal Grids," *Red Blob Games*, 2013. [Online]. Available: https://www.redblobgames.com/grids/hexagons/

[20] J. A. Boron, "Developing Combat Behavior Through Reinforcement Learning in Wargames and Simulations," M.S. Thesis, Naval Postgraduate School, 2020. (※ 서론 [18]과 동일 문헌인 경우 단일 번호로 통합)

[21] A. Yang, B. Yang, B. Zhang, B. Hui, B. Zheng, B. Yu, C. Li, D. Liu, F. Huang, H. Wei et al., "Qwen2.5 Technical Report," *arXiv preprint*, arXiv:2412.15115, 2024.

[22] J. Wei, X. Wang, D. Schuurmans, M. Bosma, B. Ichter, F. Xia, E. H. Chi, Q. V. Le, and D. Zhou, "Chain-of-Thought Prompting Elicits Reasoning in Large Language Models," in *Advances in Neural Information Processing Systems (NeurIPS 2022)*, 2022.

[23] S. Yao, J. Zhao, D. Yu, N. Du, I. Shafran, K. Narasimhan, and Y. Cao, "ReAct: Synergizing Reasoning and Acting in Language Models," in *Proceedings of the Eleventh International Conference on Learning Representations (ICLR 2023)*, 2023.

[24] L. Huang, W. Yu, W. Ma, W. Zhong, Z. Feng, H. Wang, Q. Chen, W. Peng, X. Feng, B. Qin, and T. Liu, "A Survey on Hallucination in Large Language Models: Principles, Taxonomy, Challenges, and Open Questions," *ACM Transactions on Information Systems*, 2023.
